{
  "private": true,
  "browserslist": [
    "extends @wordpress/browserslist-config"
  ],
  "scripts": {
    "dev": "sage build --mode development",
    "build": "sage build --mode production",
    "production": "sage build --mode production --ci"
  },
  "devDependencies": {
    "@roots/sage": "^3.0.18-alpha.0",
    "@wordpress/browserslist-config": "^2.7.0"
  },
  "dependencies": {
    "bootstrap": "^4.5.0",
    "jquery": "^3.5.0",
    "popper.js": "^1.16.1"
  }
}
