{
  "private": true,
  "browserslist": [
    "extends @wordpress/browserslist-config"
  ],
  "scripts": {
    "dev": "sage build --mode development",
    "build": "sage build --mode production",
    "debug": "sage build --mode production --log --debug --ci"
  },
  "devDependencies": {
    "@roots/sage": "^3.0.28-alpha.0",
    "@types/webpack-env": "^1.16.0",
    "@wordpress/browserslist-config": "^2.7.0"
  },
  "dependencies": {
    "bootstrap": "^4.5.0",
    "popper.js": "^1.16.1"
  }
}
